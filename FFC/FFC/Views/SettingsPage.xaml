<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:FFC.ViewModels"
             mc:Ignorable="d"
             x:Class="FFC.Views.SettingsPage"
             Title="{Binding Title}">
    
    <ContentPage.BindingContext>
        <vm:SettingsPageViewModel/>
    </ContentPage.BindingContext>

	<Grid>
	<Grid.RowDefinitions>
		<RowDefinition Height="*"/>
		<RowDefinition Height="Auto"/>
	</Grid.RowDefinitions>


		<TableView Intent="Settings" Grid.Row="0">
			<TableRoot>
				<TableSection Title="My settings">
					<SwitchCell Text="Light Theme" On="true"/>
				</TableSection>
			</TableRoot>
		</TableView>

		<Button Text="Log out" Margin="20 ,20,0,20" HeightRequest="50"
		        VerticalOptions="End" WidthRequest="220" FontSize="16" 
		        HorizontalOptions="Center" Grid.Row="1">
		</Button>

	</Grid>
</ContentPage>